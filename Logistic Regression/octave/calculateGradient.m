function gradient = calculateGradient(weight, X, Y, regLambda)
%Calculate the gradient of the objective function
%Input: X(n-by-(d+1) matrix), Y(n-by-1 matrix), 
%regLambda(scalar, regularization constant), weight((d+1)-by-1 matrix)
%output: gradient(an (d+1)-by-1 matrix)


end