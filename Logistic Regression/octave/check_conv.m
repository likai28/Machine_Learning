function statement = check_conv(weight, new_weight, epsilon)
%Check convergence based on tolerance values
% weight, new_weight ((d+1)-by-1 matrix)
%Return true if the weights have converged, otherwise return false.

statement = false;
end